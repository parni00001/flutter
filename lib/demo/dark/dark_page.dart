import 'package:flutter/material.dart';

class DarkPage extends StatelessWidget {
  const DarkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("dark"),);
  }
}
